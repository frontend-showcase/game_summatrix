{
  "name": "game_summatrix",
  "version": "1.0.0",
  "description": "Summatrix - игра на чистом JS",
  "homepage": "https://https://frontend-showcase.github.io//game_summatrix",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --dist-dir dist --public-url ./",
    "clean": "rimraf dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.4",
    "parcel": "^2.15.4",
    "rimraf": "^6.0.1",
    "sass": "^1.90.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
